version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=postgresql://neondb_owner:npg_7ucKZsm2Byrz@ep-rapid-bar-aodhkiej-pooler.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
      - JWT_SECRET=supersecuritycampusissuetracker2026
      - PORT=5000
    depends_on:
      - frontend

  frontend:
    build: ./frontend
    ports:
      - "80:80"
